{
  "name": "smart-spawn-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "live:smoke": "bun run scripts/live-smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
